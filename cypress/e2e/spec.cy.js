import { Action } from "../support/actions/action";

context("viewDashboard", () => {
  const action = new Action();
  beforeEach(() => {
    action.c2cLogin();
  });

  it("Successfully logs in", () => {
    
  })
  

  it("Opens Add Client Window", () => {
    
  })
})

